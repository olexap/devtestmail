# Copy this file to .env and customise before starting the container.
# The .env file is listed in .gitignore so credentials are not committed.
MAIL_USER=testuser
MAIL_PASS=testpass
MAIL_DOMAIN=devtestmail.local
